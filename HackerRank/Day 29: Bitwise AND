public static int bitwiseAnd(int N, int K) {
    // Write your code here
    
    int max=0;
    for(int i=1;i<N;++i)
    {
        for(int j=i+1;j<=N;++j)
        {
            int tmp = i&j;
            if(tmp<K && tmp>max)max=tmp;
        }
    }
    return(max);

    }
