class Solution {
    public int countOdds(int low, int high) {
        if(high==0)return 0;

        if(low%2!=0 || high%2!=0){
            --low;++high;
        }

        return((high-low)/2);
        
    }
}
