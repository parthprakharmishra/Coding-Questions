class MyCalendar {
    
    
    TreeMap<Integer, Integer> calen = new TreeMap<>();
    
    public MyCalendar() {
        calen.put(Integer.MAX_VALUE, Integer.MAX_VALUE);
    }
    
    public boolean book(int start, int end) {
        Map.Entry<Integer,Integer> i = calen.higherEntry(start);
        boolean res = end <= i.getValue();
        if(res)calen.put(end, start);
        return res;
    }
}

/**
 * Your MyCalendar object will be instantiated and called as such:
 * MyCalendar obj = new MyCalendar();
 * boolean param_1 = obj.book(start,end);
 */
