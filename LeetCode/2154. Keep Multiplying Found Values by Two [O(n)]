class Solution {
    public int findFinalValue(int[] nums, int o) {

        Set<Integer> hm = new HashSet<>();
        for(int i=0;i<nums.length;++i){
            hm.add(nums[i]);
        }

        while(hm.contains(o)){
            o*=2;
        }
        return(o);
        
    }
}
